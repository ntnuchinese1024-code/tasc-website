---
interface Props {
  size?: number;
  class?: string;
}
const { size = 40, class: className = "" } = Astro.props;
---

<img
  src="/logo.png"
  alt="台灣性諮商學會 TASC 標誌"
  width={size}
  height={size}
  class={className}
  style={`width:${size}px; height:${size}px; object-fit:contain;`}
/>
