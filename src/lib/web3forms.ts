/**
 * Shared Web3Forms config for every form on this site (contact, course
 * registration, etc). One free access key covers the whole site — get one
 * at https://web3forms.com by verifying the association's own inbox
 * (tasc.tw@gmail.com), then paste it here. Every form using this constant
 * activates at once; nothing else needs to change.
 */
export const WEB3FORMS_ACCESS_KEY = "REPLACE_WITH_REAL_KEY";

export const WEB3FORMS_READY = WEB3FORMS_ACCESS_KEY !== "REPLACE_WITH_REAL_KEY";
